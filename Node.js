/**
 * Contains the value property and link to the 'nextNode', both are set as null by default.
 */

class Node {
  constructor(value) {
    this.value = value;
    this.next = null;
  }
}

export default Node;
